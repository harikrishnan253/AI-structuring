# S4Carlisle Pre-Editor v3 Configuration
# Copy this to .env and update values

# ============================================
# Required
# ============================================
GOOGLE_API_KEY=your_google_api_key_here

# ============================================
# Queue Mode
# ============================================
# threading = Simple mode, no external services (for development)
# celery = Production mode with Redis (recommended)
QUEUE_MODE=celery

# ============================================
# Redis (required for celery mode)
# ============================================
REDIS_URL=redis://localhost:6379/0

# ============================================
# Flask Configuration
# ============================================
FLASK_DEBUG=false
SECRET_KEY=change-this-to-a-long-random-string

# ============================================
# Paths
# ============================================
UPLOAD_FOLDER=uploads
OUTPUT_FOLDER=outputs
DATABASE_PATH=preeditor.db

# ============================================
# Processing Settings
# ============================================
CONFIDENCE_THRESHOLD=85
MAX_PARAGRAPHS_PER_CHUNK=100

# ============================================
# AI Model Configuration
# ============================================
# Primary Gemini model for classification
# Options:
#   - gemini-2.5-flash (RECOMMENDED - best accuracy)
#   - gemini-2.5-flash-lite (faster, lower cost, lower accuracy)
#   - gemini-2.0-flash (older generation)
GEMINI_MODEL=gemini-2.5-flash

# Fallback model (if primary fails or times out)
GEMINI_FALLBACK_MODEL=gemini-2.5-flash

# Model parameters (fine-tuning)
# Temperature: 0.0 (deterministic) to 1.0 (creative)
GEMINI_TEMPERATURE=0.1

# Top P: Nucleus sampling (0.0 to 1.0)
GEMINI_TOP_P=0.95

# Top K: Consider top K tokens (1 to 100)
GEMINI_TOP_K=40

# Max output tokens
GEMINI_MAX_TOKENS=8192

